{
  "_note": "This manifest is descriptive only — actual enforcement happens via AGENTS.md's Environment Hook Check, which instructs the agent to run session-start.sh directly. See hooks/README.md.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "hooks/session-start.sh"
          }
        ]
      }
    ]
  }
}
